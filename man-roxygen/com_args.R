#' @param null_model (integer) which null model to use. See Details.
#' @param randomizations (numeric) number of randomizations. Default: 999
#' @param abundance (logical) If \code{TRUE} (default) computed accounting
#' for abundance. Otherwise, uses presence-absence.
